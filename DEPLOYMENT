Мини-мануал по деплою:
1. Тестовое задание было написано на PHP 7.2, WP 5+ 
(на совместимость с PHP 5.6 не тестировалось, если надо, могу переписать под минорную ветку);
2. Дамп базы лежит в корне (wp-test.sql);
3. Сделал экспорт группы ACF-полей "Release params", файл называется "Release params.json"
(нужны для работы, убедиться, что эти поля есть, без них не будет работать);
4. В wp-config.php должны быть прописаны настройки подключения к базе 
и установлены константы "EMPTY_DATA_TEXT", "RELEASES_PER_PAGE";
5. Выставить sudo chmod -R 777 для папки темы (/wp-content/themes/my/), также не забыть в админке переключиться на эту тему;
6. В разделе Настройки - Чтение выставить страницу "Home page" как главную страницу сайта;
7. В разделе Настройки, парметр "Формат даты" - d.m.Y, "Формат времени" - H:i;
8. Активировать все добавленные плагины;
9. Проверить что .htaccess работают (т.е включен mod_rewrite и все файлы .htaccess на месте). 
Это если web-server Apache (для других сделать аналогичные действия);
10. Создать кастомные типы постов "release" и "artist" 
(сделал импорт, чтобы вручную не создавать, файл называется "release_and_artist.json");
11. Создать кастомную таксономию "genre" (сделал импорт, файл называется "genre-taxonomy.json");
12. Не забыть включить тему fino в папку themes (на ней основывается тема my).

WP Admin:
login: admin
password: yfH%GfggA!IakPlSKy

Если не получается развернуть - пишите я помогу.
